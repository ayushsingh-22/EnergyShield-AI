"""Latest event feed and event detail endpoints (Phase 4 geopolitical event extraction agent).

See docs/API_REFERENCE.md for the endpoints this router will own.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/events", tags=["events"])

# TODO(Phase 4): implement endpoints listed in docs/API_REFERENCE.md
