"""Executive crisis-response report generation endpoints (Phase 8 backend platform APIs).

See docs/API_REFERENCE.md for the endpoints this router will own.
"""

from __future__ import annotations

from fastapi import APIRouter

router = APIRouter(prefix="/reports", tags=["reports"])

# TODO(Phase 8): implement endpoints listed in docs/API_REFERENCE.md
